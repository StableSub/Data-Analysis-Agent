# AI Agent

AI Agent의 역할과 실행 방식을 설명하는 문서를 모아두는 디렉터리다.

- `trace-and-logging.md`: Trace 및 로깅 구조

AI Agent의 일반 실행 흐름은 중복 문서로 분리하지 않고 `../request-lifecycle.md`를 기준으로 본다.
감사/분석 성격 문서는 기본 architecture 컨텍스트가 아니므로 `../../analysis/`에 보관한다.
